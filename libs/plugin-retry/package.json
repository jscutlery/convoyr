{
  "name": "@convoy/plugin-retry",
  "version": "2.2.0",
  "license": "MIT",
  "private": false,
  "repository": "git@github.com:jscutlery/convoy.git",
  "scripts": {
    "prepublishOnly": "rm -rf dist && cp -r ../../dist/libs/plugin-retry dist",
    "postpublish": "rm -r dist"
  },
  "peerDependencies": {
    "@convoy/core": "^2.0.0",
    "rxjs": "^6.5.4"
  },
  "dependencies": {
    "backoff-rxjs": "^6.3.3"
  }
}
