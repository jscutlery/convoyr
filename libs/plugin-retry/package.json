{
  "name": "@convoyr/plugin-retry",
  "version": "3.0.0",
  "license": "MIT",
  "private": false,
  "repository": "git@github.com:jscutlery/convoyr.git",
  "scripts": {
    "prepublishOnly": "rm -rf dist && cp -r ../../dist/libs/plugin-retry dist",
    "postpublish": "rm -r dist"
  },
  "peerDependencies": {
    "@convoyr/core": "^2.0.0",
    "rxjs": "^6.5.4"
  },
  "dependencies": {
    "backoff-rxjs": "^6.3.3"
  }
}
